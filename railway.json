{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm run build && node scripts/local-db-update.mjs"
  },
  "deploy": {
    "preDeploy": "pnpm run seed:correct && node scripts/update-db-local-images.mjs || true",
    "startCommand": "bash scripts/force-db.sh && pnpm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
